# 只有「打包成獨立執行檔」時才需要，不是這支服務執行期間的依賴——
# 跑 python3 app.py（或最終打包出來的凍結執行檔）都用不到這個檔案，
# 只有要重新產生 dist/ 底下那份可散布的執行檔時才需要裝。
#
# 用法：
#   pip install -r requirements.txt -r requirements-build.txt
#   （完整的重建步驟見 README.md「打包成獨立執行檔」一節）
pyinstaller==6.22.0

# paddle.utils.cpp_extension（OCR 推論本身用不到，但 paddle 套件初始化
# 時就會 import 到）需要 Cython 的 Utility/*.cpp 樣板檔案，PyInstaller
# 靠 --collect-data Cython 把它們收進打包結果——這代表打包當下的環境
# 必須裝有 Cython 本體（不只是拿到那幾個檔案），見 README.md 的說明。
Cython==3.2.9
